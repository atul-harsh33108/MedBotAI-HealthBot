# HealthBot dependencies.
#
# Kept in sync with the [project.dependencies] table in pyproject.toml so that
# the setup step from the project brief:
#
#     uv add -r requirements.txt
#
# reproduces the same environment. Lower bounds match the versions this project
# was built and verified against; uv.lock holds the exact resolved set, so
# `uv sync` is the reproducible install if you have the lock file.

# --- Core: LangGraph workflow and LangChain plumbing -------------------------
langgraph>=1.2.11
langchain>=1.3.18
langchain-community>=0.4.2        # provides the Tavily tool used by the project

# --- LLM providers (any one key is enough; the notebook offers what it finds) -
langchain-openai>=1.6.0
langchain-google-genai>=4.3.7
langchain-anthropic>=1.7.0
openai>=3.5.0
anthropic>=1.2.0
google-generativeai>=0.8.6

# --- Search -----------------------------------------------------------------
tavily-python>=0.8.0

# --- Notebook runtime (needed for the kernel and for input()/display()) ------
jupyter>=1.1.1
ipykernel>=7.3.0

# --- Configuration ----------------------------------------------------------
python-dotenv>=1.2.3

# --- Structured model output (grading schema + citation verification) --------
# A transitive dependency of langchain, but the notebook imports it directly, so
# it is declared here rather than relied on implicitly.
pydantic>=2.13.5

# --- Used only by the API connectivity scripts (test_api_keys*.py) -----------
requests>=2.34.2
boto3>=1.43.83
cohere>=7.1.0
huggingface-hub>=1.29.0
langfuse>=4.15.1
